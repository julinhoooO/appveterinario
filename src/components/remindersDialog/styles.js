import styled from 'styled-components/native';
import {List} from 'react-native-paper';

export const Item = styled(List.Item).attrs({
  titleStyle: {
    fontSize: 16,
  },
})``;

export const ItemIcon = styled(List.Icon)``;
